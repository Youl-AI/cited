# 4단계 Task 12 — 수동 게이트 체크리스트 (2026-08-03 실행 완료)

> 2026-08-03 병합(`f9b080f`) 시점 작성, 같은 날 실행. Task 0~12 전부 완료.
> 실행 기록: E2E는 `docs/superpowers/notes/task12-step1-6-report.md`,
> 실측 루프·발견 사항은 아래 "실행 결과" 절.

## 사전 조건

- [x] GitHub 저장소 Actions 시크릿 `CRON_SECRET` 등록 — 1차 등록값이 Vercel과 불일치(401)라 재생성·3곳 통일
- [x] Anthropic 크레딧 충전
- [x] Vercel 배포 확인

## E2E + 실측 루프 — 전부 통과

- [x] E2E 3종: 게이트 spec 2(CI-safe) + 온보딩 완주 1(LLM 가짜 주입, 로컬 게이트) — 커밋 `e05ed05`, 프로덕션 원상복구 검증
- [x] 실계정 온보딩 완주 (무신사·패션·경쟁사 3) — AI 생성 실사용, 동결, 프로덕션 UI
- [x] 크몽 전환: `plan:grant --from-audit`(리본요가 deluxe) → 브랜드·업종 프리필 + 동결 질의 10개 그대로, 처음 3개 잠금
- [x] 중도 이탈 재개 경로 실전 확인 (재로그인 → 에디터 복귀)
- [x] `workflow_dispatch` 실측 1회 — run `aaf7e51e` succeeded, 60답변, 264초, 스냅샷 저장
- [x] 대시보드: 헤드라인 77% (65%~86%) + 추이 점 1 + 히트맵 10행("못 나오는 것부터" 정렬) + SoV + 출처 8도메인 + 회차 목록/상세
- [x] 실패 시나리오: 로컬 :3100 + 깨진 키 → attempt 1 실패 → attempt 2 실패 → 3차 호출 건너뜀(2/일 상한 작동). 실패 메일 실발송 확인("[Cited 운영] 정기 측정 실패")
- [ ] 월·수·금 스케줄 첫 주 실행 이력 대조 — **다음 주 확인 남음** (수·금 새벽 Actions 이력과 회차 수)

## 브라우저 디자인 확인 — 통과 (스크린샷 기반)

- [x] 375px: body 가로 넘침 없음, 히트맵 정상 — 단 추이·SoV x축 라벨 겹침은 점 1개라 미검증(회차 쌓인 뒤 재확인)
- [x] 히트맵 램프 대비: 2/6~6/6 실데이터에서 시각 확인, 진한 셀 흰 글자 정상
- [x] reduced-motion: playwright 에뮬레이션에서 전 transition 0.01ms 확인 (전역 킬 스위치 실동작)
- [x] 온보딩·대시보드 조판(아이브로·mono 계측값·keep-all) 스크린샷 확인
- [ ] gemini/google 마커 판독성 — google 엔진 미도입이라 해당 없음(SerpApi 때 확인)

## 실행에서 발견·수정한 것 (계획이 못 잡았던 배포 결함 3건)

1. **Vercel에 LLM API 키 3종 부재** — 지금까지 서버가 LLM을 쓸 일이 없었음(측정은 전부 로컬 CLI).
   4단계가 처음으로 서버에서 호출 → 생성·측정 전부 실패했을 것. 운영자가 대시보드에서 추가.
2. **CRON_SECRET 3곳 불일치** — Vercel 값이 유실돼 있었음. 재생성·통일. (첫 스케줄 발화 2건이 401로 죽어 있었음)
3. **측정이 Vercel 300초 한도 초과** — 동시성 4로는 브랜드 1개 수집이 프로덕션에서 300초+ (Hobby 상한 300초 고정).
   run 1개가 함수 킬로 죽으며 부분 과금(~수백 원). LLM 동시성 4→8로 조정(`80e33a1`) → 264초 완주.
   **여유 36초 — 얇다.** 429 백오프 한 번이면 넘는다. 재발 시 측정을 호출 여러 번에 나누는 구조 변경 필요.

## 남은 운영 확인 (다음 주)

- 수(8/5)·금(8/7) 새벽 스케줄이 실제로 도는지 Actions 이력 확인 — 단, 현재 측정 대상 브랜드 0개(테스트 구독 회수됨)라 idle green이 정상
- 테스트 데이터: e2e-onboarding@ 계정(canceled)에 무신사 측정 1회가 남아 있음 — 시각 격상 튜닝용 실데이터. 필요 없어지면 `pnpm e2e:onboarding:cleanup`

## 알려진 이연·백로그 (병합 시점 확정분)

- `result-view.tsx:309` 경쟁사 배지 `text-incomplete-fg` — **의도적 이연**: ResultView는 동결된 유료 리포트/PDF와 공유. 리포트·대시보드가 같이 뒤집히는 리트로핏 때 수정
- shadcn 프리미티브(button/badge/tabs) `transition-all` + 기본 이징 — §5 위반이나 공유 컴포넌트 경유. 리트로핏 백로그
- 브랜드 폼: 멀티도메인 입력 없음(프리필이 `selfDomains[0]`만) · 경쟁사 한도 초과분 조용히 절단(SoV 우호 방향) — 폼 개선 때 함께
- `points` 페이로드 무상한 (Business `historyMonths: null` — 주 3회 × 1년 ≈ 156 스냅샷/브랜드). 1년 내 상한 또는 페이지네이션
- 게이트가 페이지당 neon-http 3~4왕복 — 성능 백로그
- 디자인 grep 배터리에 raw-palette·이징 누락 패턴 없음 — 다음 디자인 작업 때 보강
- `judgeChange`가 n=0 표본에도 `incomparable` 반환 — 일반화 문장이 그 경우 부정확 (기존 결함)
- audit-templates.test.ts:160 주석 "15분 간격" (단언 아님)

## 구조적 미해결 (결제 도입 때 닫음)

- `currentPeriodEnd` null — 수동 청구 미납을 시스템이 모름. cron은 `status IN ('active','past_due')`로 계속 측정하며 원가만 태움. Toss 도입 때 만료 개념과 함께
- 동시 동결 quota 창 (두 브랜드가 각자 `queriesOnOtherBrands=0`을 읽는 레이스) — 스키마 변경 필요
- `--from-audit` 같은 진단을 두 구독이 물 수 있음 (운영자 CLI 전용이라 경미)
