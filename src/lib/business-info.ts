/**
 * 전자상거래법상 통신판매업자 표시 의무 항목 + 서비스 연락처.
 *
 * 사업자 등록이 끝나지 않은 항목은 빈 문자열로 둔다(`null`이 아니다 —
 * `business-info.test.ts`의 `.not.toBe('')` 검증이 빈 문자열만 실패로
 * 잡아낸다. `null`로 두면 `null !== ''`가 항상 참이라 테스트가 절대
 * 실패하지 않고, 그러면 "채우는 걸 잊었는지"를 코드가 감지하지 못한다).
 *
 * 이 값은 푸터(`site-footer.tsx`)와 이용약관·개인정보처리방침, 그리고
 * 4단계 결제 화면(토스페이먼츠 결제창에 표시할 사업자 정보)이 그대로 쓴다.
 * 사업자 등록이 끝나면 여기만 채우면 된다 — 소비처를 따로 고칠 필요가 없다.
 *
 * 빈 문자열 항목은 **두 묶음**으로 나뉜다. 근거 법률이 다르고 의무 발생 시점도
 * 다르다 — `business-info.test.ts`가 그 경계를 지킨다.
 *  - 개인정보보호법 제30조(보호책임자): **개인정보 수집 시점**부터 의무.
 *    이미 발생했으므로 채워져 있고 테스트가 상시 검증한다.
 *  - 전자상거래법 제10조(사업자 신원정보): **판매 시작 시점**부터 의무.
 *    아직이라 비어 있고 테스트는 skip 상태다.
 *
 * 2026-07-30: 사업자 등록 완료(372-31-02135). 상호·대표자명·등록번호·소재지를
 * 채웠고 테스트의 상시 검증 묶음으로 옮겼다.
 *
 * 남은 것은 **통신판매업 신고번호 하나**다. 이것은 사업자 등록과 **별개 절차**로,
 * 등록증이 나왔다고 생기지 않는다 — 관할 시·군·구청(평택시청)에 따로 신고해야
 * 한다. 무료 서비스만 제공하는 동안은 신고 의무 자체가 없다(전자상거래법 제12조
 * 제1항 — 통신판매업 신고 의무는 재화·용역의 **판매**를 전제한다).
 *
 * TODO(phase-4): 통신판매업 신고번호를 채우면 `business-info.test.ts`의
 * `describe.skip('BUSINESS_INFO — 유료 오픈 전 필수', ...)`를 `describe(...)`로
 * 되돌려 테스트가 다시 검증하게 한다.
 * (4단계 계획서 `docs/superpowers/plans/2026-07-28-cited-phase-4-billing-and-onboarding.md`의
 * "착수 전 확인" 체크리스트에도 같은 항목이 있다 — 여기 마커는 grep으로 이 파일을
 * 먼저 찾아오는 사람을 위한 것이다.)
 */
export const BUSINESS_INFO = {
  serviceName: 'Cited',
  /**
   * 상호 — 사업자 등록증의 '상호'.
   *
   * ★ `serviceName`('Cited')과 따로 둔다. 약관·푸터의 법적 주체는 등록된 상호인
   *   '사이티드'이고, 제품 이름은 'Cited'다. 하나로 합치면 약관이 "Cited가
   *   제공하는 Cited"가 되거나, 반대로 랜딩에 '사이티드'가 나온다.
   */
  companyName: '사이티드',
  /** 대표자명 — 개인사업자라 `privacyOfficer`와 같은 사람이다 */
  representative: '김하율',
  /**
   * 사업자등록번호 000-00-00000.
   *
   * ★ 비밀 값이 아니다. 전자상거래법 제10조가 **공개를 의무로 하는** 정보이고
   *   국세청 사업자등록상태 조회로 누구나 확인할 수 있다. 그래서 코드에 둔다.
   *   `.env`로 옮기면 푸터·약관·결제창이 빌드 환경마다 달라진다.
   *
   * 표기는 하이픈 포함 형식으로 고정한다. 국세청 조회 API와 토스페이먼츠는
   * 하이픈 없는 10자리를 받으므로, 보내는 쪽에서 `businessNumberDigits`를 쓴다.
   */
  businessNumber: '372-31-02135',
  /** 통신판매업 신고번호 — 무료 서비스만 제공하는 동안은 신고 의무가 없다.
   *  4단계(유료 결제 오픈) 전에 신고하고 채운다. */
  mailOrderNumber: '',
  /**
   * 사업장 소재지 — 사업자 등록증의 주소.
   *
   * ★ 이 값은 푸터에 **공개 노출된다.** 전자상거래법 제10조 제1항이 요구하는
   *   법정 표시 항목이라 가릴 수 없다. 사업장을 옮기면 여기도 바꿔야 한다 —
   *   등록증과 다른 주소를 표시하는 것이 표시 자체를 빠뜨리는 것과 같은 위반이다.
   */
  address: '경기도 평택시 서정로 3, 나동 201호 (서정동)',
  /** 실제 메일함은 아직 없다 — 개설 전까지도 문서·발신 표기용으로 이 주소를 쓴다 */
  email: 'contact@cited.co.kr',
  /** 대표 연락처(전화). 개인정보 보호책임자 연락처로도 쓰인다
   *  (개인정보보호법 시행령 제31조 제1항 — 이 의무는 사업자 등록이 아니라
   *  **개인정보 수집 시점**에 발생하므로 등록 전에도 채워야 한다).
   *  푸터와 개인정보처리방침 양쪽에 공개 노출된다. */
  phone: '010-7469-1727',
  /** 개인정보 보호책임자(개인정보보호법 제30조 제1항 제5호). 직책은
   *  개인정보처리방침 §11에 '대표'로 고정 서술한다 — 1인 운영이라 부서가 없다. */
  privacyOfficer: '김하율',
  hostingProvider: 'Vercel Inc.',
} as const

export type BusinessInfo = typeof BUSINESS_INFO

/** 하이픈을 뗀 10자리. 국세청 조회 API와 토스페이먼츠가 이 형태를 받는다. */
export const businessNumberDigits = BUSINESS_INFO.businessNumber.replace(/\D/g, '')

/**
 * 사업자등록번호 검증자리 확인 (국세청 알고리즘).
 *
 * 자리수만 세면 오타를 못 잡는다. 이 번호는 푸터·약관·결제창에 그대로 나가고,
 * 틀린 번호를 표시하는 것은 전자상거래법 제10조 위반이면서 동시에 고객에게
 * "조회했는데 없는 사업자"로 보이는 문제다 — 결제 직전에 신뢰를 깬다.
 *
 * 앞 9자리에 가중치 [1,3,7,1,3,7,1,3,5]를 곱해 더하고, 9번째 자리의 5배 중
 * 십의 자리를 한 번 더 더한 뒤, 10에서 그 합의 일의 자리를 뺀 값이 10번째
 * 자리와 같아야 한다.
 */
export function isValidBusinessNumber(value: string): boolean {
  const digits = value.replace(/\D/g, '')
  if (!/^\d{10}$/.test(digits)) return false
  // ★ '0000000000'은 검증자리까지 맞는다(합 0 → 검증자리 0). 그런데 이건
  //   JSDoc의 `000-00-00000` 자리표시자를 그대로 붙여넣은 경우다. 알고리즘이
  //   통과시키는 유일한 자리표시자라 명시적으로 막는다.
  if (/^0{10}$/.test(digits)) return false

  const weights = [1, 3, 7, 1, 3, 7, 1, 3, 5] as const
  let sum = 0
  for (const [i, weight] of weights.entries()) {
    sum += Number(digits[i]) * weight
  }
  // 9번째 자리(index 8)는 가중치 5가 이미 곱해졌고, 그 곱의 십의 자리를 더한다.
  sum += Math.floor((Number(digits[8]) * 5) / 10)

  return (10 - (sum % 10)) % 10 === Number(digits[9])
}
